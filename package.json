{
  "scripts": {
    "build": "node -e \"const fs=require('fs'); ['index.html','style.css','app.js','manifest.json'].forEach(f => fs.copyFileSync(f, 'dist/'+f));\"",
    "sync": "npm run build && npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}
